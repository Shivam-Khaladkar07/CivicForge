# Set in Vercel Project > Settings > Environment Variables (Production).
# Paste the NEW Render API service's actual HTTPS origin, without /api.
# This is deployment routing configuration, not a VITE_* browser variable.
# Local Vite development already proxies /api to localhost:4000.
CIVICFORGE_API_ORIGIN=
